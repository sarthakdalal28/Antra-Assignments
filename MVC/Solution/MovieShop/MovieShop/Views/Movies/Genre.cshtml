@*@using ApplicationCore.Entities
@model PaginatedResultSet<MovieCardModel>

<div class="row">
    @foreach (var movie in Model.Data)
    {
        <div class="col-md-3">
            @await Html.PartialAsync("_MovieCard", movie)
        </div>
    }
</div>

<div class="row">
    <div class="col-md-12 text-center">
        <ul class="pagination">
            <li class="page-item @(Model.HasPreviousPage ? "" : "disabled")">
                <a class="page-link" asp-action="MoviesByGenre" asp-route-id="@Model.GenreId" asp-route-pageNumber="@(Model.PageNumber - 1)" asp-route-pageSize="@Model.PageSize">Previous</a>
            </li>
            <li class="page-item @(Model.HasNextPage ? "" : "disabled")">
                <a class="page-link" asp-action="MoviesByGenre" asp-route-id="@Model.GenreId" asp-route-pageNumber="@(Model.PageNumber + 1)" asp-route-pageSize="@Model.PageSize">Next</a>
            </li>
        </ul>
    </div>
</div>*@






@using ApplicationCore.Entities
@model PaginatedResultSet<MovieCardModel>
@{
    var genreId = ViewBag.GenreId;
}

<div class="container">
    <div class="row">
        <h2>Movies By Genre</h2>
        @foreach (var movie in Model.Data)
        {
            <div class="col-md-3">
                <partial name="_MovieCard" model="movie" />
            </div>
        }
    </div>

    <div class="row">
        <div class="col-md-12">
            <nav aria-label="Page navigation">
                <ul class="pagination">
                    @if (Model.PageNumber > 1)
                    {
                        <li class="page-item">
                            <a class="page-link" asp-action="Genre" asp-route-genreId="@genreId" asp-route-pageNumber="@(Model.PageNumber - 1)" aria-label="Previous">
                                <span aria-hidden="true">&laquo;</span>
                            </a>
                        </li>
                    }
                    @for (var i = 1; i <= Model.TotalPages; i++)
                    {
                        <li class="page-item @(Model.PageNumber == i ? "active" : "")">
                            <a class="page-link" asp-action="Genre" asp-route-genreId="@genreId" asp-route-pageNumber="@i">@i</a>
                        </li>
                    }
                    @if (Model.PageNumber < Model.TotalPages)
                    {
                        <li class="page-item">
                            <a class="page-link" asp-action="Genre" asp-route-genreId="@genreId" asp-route-pageNumber="@(Model.PageNumber + 1)" aria-label="Next">
                                <span aria-hidden="true">&raquo;</span>
                            </a>
                        </li>
                    }
                </ul>
            </nav>
        </div>
    </div>
</div>

